"use strict";
/*
 *  tsc --init:           cria um arquivo de configuração para o ts
 *  tsc meuprimeiro.ts:   compila o arquivo para o js;
 *  node meuprimeiro.js:  roda ele no node;
 *  tsc -w:               gera todos os arquivos ts em js e fica assistindo as modificações
 *  let:                  palavra reservada; utilizada para declarar variáveis com escopo de bloco;
 *  const:                usada para declarar variáveis read-only, ou seja, seu valor não pode ser alerado;
 *
 *
 */
var a = 'hello world';
console.log(a);
/**
 *      GIT
 *      - git init: inícia o repositório
 *      - git status: seu arquivos não está tranqueado
 *      - git add .: adiciona todos os arquivos
 *      - git commit -m:
 *
 *      Fazer um repositório no gitHub e copiar a url do projeto.
 *      - git remote add origin linkDoRepositorio: envia para o gitHub
 *      - git push origin nomeDaBranch: envia alterações para o repositório
 *
 */
